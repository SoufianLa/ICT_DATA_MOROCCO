{% extends '::base.html.twig' %}

{% block body -%}
    <h1>D_trafic list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Tradate</th>
                <th>Travoixmobilesort</th>
                <th>Trasmsmobilesort</th>
                <th>Travoixfixesort</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('d_trafic_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.traDate %}{{ entity.traDate|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.traVoixMobileSort }}</td>
                <td>{{ entity.traSmsMobileSort }}</td>
                <td>{{ entity.traVoixFixeSort }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('d_trafic_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('d_trafic_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('d_trafic_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
