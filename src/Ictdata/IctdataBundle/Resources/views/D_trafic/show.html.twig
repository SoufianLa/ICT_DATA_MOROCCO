{% extends '::base.html.twig' %}

{% block body -%}
    <h1>D_trafic</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Tradate</th>
                <td>{{ entity.traDate|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Travoixmobilesort</th>
                <td>{{ entity.traVoixMobileSort }}</td>
            </tr>
            <tr>
                <th>Trasmsmobilesort</th>
                <td>{{ entity.traSmsMobileSort }}</td>
            </tr>
            <tr>
                <th>Travoixfixesort</th>
                <td>{{ entity.traVoixFixeSort }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('d_trafic') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('d_trafic_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
